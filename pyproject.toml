[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "ecodata-extraction"
version = "0.1.0"
description = "Provenance-aware ecological literature-to-data workflow"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["src"]
